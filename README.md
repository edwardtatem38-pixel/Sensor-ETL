# Sensor-ETL
Real time ETL pipeline bridging Arduino hardware to SQL server via Python. Extracts ultrasonic and humidity sensor data, transforms the stream with timestamps, and loads records into structured database. Features hardware software synchronization, schema evolution, and live logging for sensors 
